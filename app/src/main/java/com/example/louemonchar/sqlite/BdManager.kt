
package com.example.louemonchar.sqlite
/*
import BDVoitures
import android.content.Context
import com.example.louemonchar.sourceDonnees.SourceDeVoituresBidon

object BdManager {
    private var database: BDVoitures? = null

    fun initialize(context: Context) {
        if (database == null) {
            val sourceDeVoitures = SourceDeVoituresBidon(context.applicationContext)
            database = BDVoitures(context.applicationContext, sourceDeVoitures)
        }
    }

    fun getBD(): BDVoitures {
        if (database == null) {
            throw IllegalStateException("BdManager doit être initialisé pour fonctionner")
        }
        return database!!
    }
}

 */
